import {makeStyles} from "@material-ui/core/styles";

export const commonStyles = makeStyles({
    prevWord: {
        display: "block",
        textAlign: "center",
        green: {
            display: "block",
            textAlign: "center"
        }
    },
    topBar:{
        textAlign:"center"
    }
})
